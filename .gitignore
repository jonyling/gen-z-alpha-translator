# Python / uv
.venv/
__pycache__/
*.pyc

# Training outputs (large, regenerated by the notebook)
outputs/
outputs_smoke/
*.safetensors

# ...but DO track the trained LoRA adapter (~50 MB) so teammates can run app.py:
!genz_lora_adapter/*.safetensors

# Unsloth auto-generated compile cache
unsloth_compiled_cache/

# Gradio cache/flagged data
.gradio/

# Large unused data (99 MB frequency file — kept locally, not in git)
data/unused/genz_slang_usage_2020_2025.csv

# OS / editor
.DS_Store
.ipynb_checkpoints/

#token
.hugging token

# Secrets / local env
.env
.env.*
!.env.example

# Grok generation audit log can get large; keep the filtered CSV if you want
data/raw/genz_grok_synthetic.raw.jsonl

# Private notes — personal, NOT for the team / not uploaded
*.private.md
*nvidia/
# Secrets
.env
